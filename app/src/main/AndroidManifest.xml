<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.clicker">

    <!-- 네트워크 및 진동 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Clicker.NoActionBar"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- AdMob 앱 ID (테스트용) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <!-- Splash 화면 -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity : 투명 상태바 -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Clicker.Transparent"
            android:exported="true" />

        <!-- SettingsActivity : 배경 #E0E0E0 -->
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/Theme.Clicker.Settings"
            android:exported="false" />

        <!-- NumberZoomActivity : 흰색 배경 -->
        <activity
            android:name=".NumberZoomActivity"
            android:theme="@style/Theme.Clicker.White"
            android:exported="false" />

        <!-- HowToUseActivity : 흰색 배경 -->
        <activity
            android:name=".HowToUseActivity"
            android:theme="@style/Theme.Clicker.White" />

    </application>
</manifest>

